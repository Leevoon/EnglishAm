# Web framework
Django==4.2.30
djangorestframework==3.16.1
django-filter==25.1

# DB driver — PyMySQL (no native compile required)
PyMySQL==1.1.3
cryptography==48.0.0

# Config & WSGI server
django-environ==0.12.0
gunicorn==23.0.0
whitenoise==6.8.2

# Error tracking (no-op when SENTRY_DSN is empty)
sentry-sdk==2.20.0

# Transitive (pinned so reproducible)
asgiref==3.11.1
sqlparse==0.5.5
cffi==2.0.0
pycparser==2.23
typing_extensions==4.15.0
